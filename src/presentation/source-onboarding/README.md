# Source Onboarding

The add-source wizard detects platform from pasted links, explains setup and credential requirements, lets the user select sync mode, saves demo-mode sources, and can run an initial sync.
