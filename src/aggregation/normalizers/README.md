# Normalizers

Normalizers convert raw connector payloads into metrics and content metrics. MVP connector normalizers live on each connector; these files mark the future split point when logic grows.
